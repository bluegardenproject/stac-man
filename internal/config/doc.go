// Package config loads and saves the optional global preferences file
// at ~/.config/stac-man/config.yaml. The file is created lazily — the
// tool runs with sensible defaults when no file exists.
package config
