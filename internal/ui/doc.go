// Package ui owns the synthwave neon look-and-feel: palette, lipgloss
// styles, gradients, and the stack tree renderer used by `sm log`.
//
// The palette is ported from github-butler so the two CLIs feel like
// a family. Stack-aware role mapping (current branch, needs-restack,
// trunk, PR states) is layered on top.
package ui
